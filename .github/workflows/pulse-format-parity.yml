name: Pulse Audio Format Parity

on:
  push:
    paths:
      - "assets/stems/pulse/**"
      - "assets/stingers/pulse/**"
      - "assets/transitions/pulse/**"
      - "tools/check_pulse_format_parity.py"
      - "tools/check_pulse_format_parity_semantics.py"
      - ".github/workflows/pulse-format-parity.yml"
  pull_request:
    paths:
      - "assets/stems/pulse/**"
      - "assets/stingers/pulse/**"
      - "assets/transitions/pulse/**"
      - "tools/check_pulse_format_parity.py"
      - "tools/check_pulse_format_parity_semantics.py"
      - ".github/workflows/pulse-format-parity.yml"
  workflow_dispatch:

jobs:
  parity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
      - name: Check decoded cross-format audio parity
        run: python tools/check_pulse_format_parity.py
      - name: Check parity gate semantics
        run: python tools/check_pulse_format_parity_semantics.py
      - name: Upload Pulse Format Parity Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pulse-format-parity-report
          path: qa/out/pulse-format-parity.json
          if-no-files-found: ignore
          retention-days: 14
